 #!/bin/sh
NF=ta3d
tgfile=`ls -l $0 | awk '{print $NF}'`
exe=`dirname $tgfile`

if [ $exe == "." ]
then
	exe=`pwd`
fi

yes=`echo $exe | head -c 2 | grep -c "/" `

if [ "$yes" == "0" ]
then
	exe=`pwd`/${exe}
fi

cd ${exe}
${exe}/ta3d-bin
